

// ✅ ตอนทดสอบในเครื่อง (Localhost) ให้เปิดบรรทัดนี้
const API_URL = "http://localhost:3001";

// 🚀 ตอนเอาขึ้นเว็บจริง (Deploy) ให้สลับมาเปิดบรรทัดนี้แทน (แล้วปิดบรรทัดบน)
// const API_URL = "https://ชื่อโปรเจคของคุณ.onrender.com";

export default API_URL;